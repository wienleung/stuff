[user]
    name = Wien Leung
    email = wien.leung25@gmail.com
[apply]
    whitespace = fix
[color]
    ui = auto
[alias]
    ci = commit
    up = update
    st = status
    br = branch
    co = checkout
    di = diff
    lg = log -p
    gl = log --graph --oneline --all --decorate
    gl2 = log --graph --pretty=format:'%C(yellow)%h%Creset -%C(red)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    gln = log --graph --oneline --all --decorate --stat
    glf = log --graph --oneline --all --decorate --name-status
    rl = reflog
    who = shortlog -s --
    w = whatchanged
[credential]
	helper = cache
